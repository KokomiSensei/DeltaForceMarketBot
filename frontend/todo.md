# 没写完的提示词：
我重新思考了dirty与tempconfig的相关逻辑。
1. 不允许用户在机器人control panel页面更改设置名称
2. 如果用户更改了加载进来的设置（比如a），则创建一个临时设置（a*）。
3. 显示apply、apply and save、save as三个按钮。如果点击了apply，则当前设置仍然为临时设置a*，将临时设置加载进机器人；如果点击了apply and save，则apply新设置，并将新设置写入a；如果点击了save as，则提示用户另存为该设置..

# 断点：
设置-机器人-持久化
三者之间状态如何同步，不同的不同步状态下显示什么样的按钮，需要仔细思考。我们改日再战
